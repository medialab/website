apiVersion: apps/v1
kind: Deployment
metadata:
  name: api
spec:
  template:
    spec:
      containers:
        - name: api
          env:
            - name: GOOGLE_ANALYTICS_ID
              value: 'UA-37695848-2'
            - name: ADMIN_URL
              value: http://localhost
            - name: BUILD_CRON
              value: '0 * * * *'
